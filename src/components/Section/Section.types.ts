import { ReactNode } from 'react';

export interface IProps {
  paddingTop: number;
  paddingBottom: number;
  children: ReactNode;
}

export interface IStyledProps {
  paddingTop: number;
  paddingBottom: number;
  children: ReactNode;
}
