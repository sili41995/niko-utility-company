const enum InputTypes {
  text = 'text',
  radio = 'radio',
  email = 'email',
  password = 'password',
  date = 'date',
  number = 'number',
  month = 'month',
  file = 'file',
}

export default InputTypes;
