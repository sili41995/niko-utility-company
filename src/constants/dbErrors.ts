const enum DbErrors {
  duplicateSubscriberAccount = 'Subscriber account already use',
  duplicateContract = 'Contract already use',
  duplicateDocument = 'Document already use',
}

export default DbErrors;
